# MyElectricalData Environment Configuration

# Weather Provider Configuration
# Options: open-meteo, weatherapi
PROVIDER=open-meteo

# Cache TTL (Time To Live) in minutes
CACHE_TTL=5

# Logging Configuration
# Options: debug, info, warning, error, critical
LOG_LEVEL=info
